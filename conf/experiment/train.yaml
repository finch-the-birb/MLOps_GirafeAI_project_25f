# @package _global_
# Train final hybrid model (gated fusion, thr=0.3%).
#
#   uv run python -m stock_forecaster.train +experiment=train

defaults:
  - override /data: thr03
  - override /model: hybrid
  - override /trainer: default
  - _self_

run_name: hybrid

paths:
  checkpoint_dir: checkpoints/hybrid
  plots_dir: plots/hybrid

mlflow:
  log_model: true
  log_plots: true
